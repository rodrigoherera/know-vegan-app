import { colors } from "./colors";
import { fonts, fontWeights, fontSizes } from "./fonts";
import { space, lineHeigths } from "./spacing";
import { sizes } from "./sizes";

export const theme = {
  colors,
  fonts,
  fontSizes,
  fontWeights,
  space,
  lineHeigths,
  sizes,
};
